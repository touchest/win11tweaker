<UserControl x:Class="Win11Tweaker.App.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>

        <Style x:Key="TabHead" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Pill" CornerRadius="8" Padding="15,9" Margin="2"
                                Background="Transparent">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="Label" Text="{Binding Title}" FontSize="13.5"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource InkDim}" />

                                <Border x:Name="BadgeHost" Margin="8,0,0,0" CornerRadius="8"
                                        Padding="6,1" VerticalAlignment="Center"
                                        Background="{StaticResource Pressed}">
                                    <TextBlock x:Name="BadgeText" Text="{Binding Badge}" FontSize="11"
                                               FontWeight="SemiBold" Foreground="{StaticResource InkDim}" />
                                </Border>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{StaticResource Elevated}" />
                                <Setter TargetName="Label" Property="Foreground" Value="{StaticResource Ink}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{Binding AccentSoft}" />
                                <Setter TargetName="Label" Property="Foreground" Value="{Binding Accent}" />
                                <Setter TargetName="Label" Property="FontWeight" Value="SemiBold" />
                                <Setter TargetName="BadgeHost" Property="Background" Value="{Binding Accent}" />
                                <Setter TargetName="BadgeText" Property="Foreground" Value="{StaticResource Base}" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Badge}" Value="">
                                <Setter TargetName="BadgeHost" Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="28,18,28,4" HorizontalAlignment="Left"
                Background="{StaticResource Surface}" BorderBrush="{StaticResource Line}"
                BorderThickness="1" CornerRadius="11" Padding="3"
                Visibility="{Binding HasTabs, Converter={StaticResource Bool2Vis}}">
            <ListBox ItemsSource="{Binding Tabs}"
                     SelectedItem="{Binding SelectedTab, Mode=TwoWay}"
                     ItemContainerStyle="{StaticResource TabHead}"
                     Background="Transparent" BorderThickness="0" Padding="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding SelectedTab}" />
    </Grid>
</UserControl>
